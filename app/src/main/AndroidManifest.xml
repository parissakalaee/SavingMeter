<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="ir.Parka.keychi">

    <uses-permission android:name="android.permission.INTERNET"/>
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">
        <activity
            android:name="ir.Parka.keychi.ActivityMain"
            android:label="@string/app_name"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme.NoActionBar"
            android:windowSoftInputMode="stateUnchanged|adjustPan">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity
            android:name="ir.Parka.keychi.ActivityHelp"
            android:theme="@style/HelpDialogStyle"></activity>
        <meta-data
            android:name="io.fabric.ApiKey"
            android:value="2b31629640ca878a85e4e24b06e63ebd9986eca3" />
    </application>

</manifest>

    <!--android:configChanges="keyboardHidden|orientation|screenSize"-->